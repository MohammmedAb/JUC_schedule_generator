import { ReactNode } from "react";
import { IconType } from "react-icons";
export declare type NoContentProps = {
    text: string;
    icon?: IconType;
    children?: ReactNode;
    noShadow?: boolean;
};
export declare function NoContent({ icon, text, children, noShadow }: NoContentProps): JSX.Element;
