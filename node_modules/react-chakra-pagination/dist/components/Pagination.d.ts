/// <reference types="react" />
import { ThemeTypings } from "@chakra-ui/react";
declare type PaginationProps = {
    onPageChange: (page: number) => void;
    currentPage: number;
    lastPage: number;
    nextPages: number[];
    previousPages: number[];
    siblingsCount: number;
    colorScheme?: ThemeTypings["colorSchemes"];
};
export declare function Pagination({ currentPage, lastPage, previousPages, nextPages, siblingsCount, onPageChange, colorScheme, }: PaginationProps): JSX.Element;
export {};
