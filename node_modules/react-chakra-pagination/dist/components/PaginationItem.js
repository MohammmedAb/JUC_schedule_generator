import * as React from "react";
import { Button } from "@chakra-ui/react";
export function PaginationItem(_a) {
    var _b = _a.isCurrent, isCurrent = _b === void 0 ? false : _b, page = _a.page, onPageChange = _a.onPageChange, colorScheme = _a.colorScheme;
    if (isCurrent) {
        return (React.createElement(Button, { size: "sm", fontSize: "xs", width: "4", colorScheme: colorScheme, disabled: true, _disabled: {
                bg: colorScheme + ".500",
                cursor: "pointer",
            } }, page));
    }
    return (React.createElement(Button, { size: "sm", fontSize: "xs", width: "4", bg: "gray.100", _hover: {
            bg: "gray.300",
        }, onClick: function () { return onPageChange(page); } }, page));
}
//# sourceMappingURL=PaginationItem.js.map