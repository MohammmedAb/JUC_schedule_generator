/// <reference types="react" />
import { ThemeTypings } from "@chakra-ui/react";
import { Column } from "react-table";
import { NoContentProps } from "./NoContent";
declare type DataType = {
    [key: string]: JSX.Element | string;
};
declare type EmptyMessage = Partial<NoContentProps>;
interface TableProps {
    columns: Column<DataType>[];
    data: DataType[];
    page: number;
    totalRegisters: number;
    onPageChange: (page: number) => void;
    colorScheme?: ThemeTypings["colorSchemes"];
    emptyData?: EmptyMessage;
}
export declare function Table({ page, onPageChange, totalRegisters, data, columns, colorScheme, emptyData, }: TableProps): JSX.Element;
export {};
